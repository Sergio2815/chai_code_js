const score=400;
console.log(score);
const balance=new Number(100)//basically if  we use this process to displat we get precisely data type as number only 
// and also get also some of the other new opeations we can perform in it
console.log(balance);

console.log